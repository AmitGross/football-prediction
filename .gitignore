# ── Generated model artefacts (included in repo for convenience) ───────────
# model.pkl, classifier.pkl, dc_ratings.pkl, calibrator.pkl are committed
# so users can run simulate2026 / evaluate without training first.

# ── Temp / eval files ──────────────────────────────────────────────────────
data/matches_eval_tmp.csv

# ── Excel exports — only keep v1.1 versioned output files ─────────────────
# Included: *_v1.1.xlsx files (current baseline)
# Excluded: old unversioned files + temp files
results_wc2022_frozen.xlsx
results_wc2022_retrain.xlsx
predictions_wc2026_full.xlsx
results_wc2026_*.xlsx
predictions_wc2026_full_with*.xlsx
predictions_wc2022_frozen.csv
predictions_wc2026.csv
~$*.xlsx

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# ── Environments ──────────────────────────────────────────────────────────
.env
.venv
env/
venv/

# ── OS ────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ───────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
